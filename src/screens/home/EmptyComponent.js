import { Body } from 'react-native-ios-kit';
import React from 'react';

class EmptyComponent extends React.PureComponent {
    render() {
        return (< Body > No notes found</Body >);
    }
}

export default EmptyComponent;